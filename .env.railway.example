# ==============================================================================
# CFATech CMS - Variáveis de Ambiente para Railway (Staging/Production)
# ==============================================================================
# Configure estas variáveis no Dashboard do Railway:
# Railway Dashboard → Project → Variables
# ==============================================================================

# ------------------------------------------------------------------------------
# AUTOMÁTICAS (Railway configura automaticamente ao adicionar PostgreSQL)
# ------------------------------------------------------------------------------
# DATABASE_URL=postgresql://user:pass@host:port/db  # Railway injeta automaticamente

# ------------------------------------------------------------------------------
# OBRIGATÓRIAS - SEGURANÇA (Gere valores únicos!)
# ------------------------------------------------------------------------------
# Use: openssl rand -base64 32 para gerar cada chave

# APP_KEYS: 4 chaves Base64 separadas por vírgula
APP_KEYS=gere-chave-1,gere-chave-2,gere-chave-3,gere-chave-4

# Salts e secrets (use valores diferentes para cada um)
API_TOKEN_SALT=gere-salt-unico-para-api-token
ADMIN_JWT_SECRET=gere-secret-unico-para-admin-jwt
TRANSFER_TOKEN_SALT=gere-salt-unico-para-transfer
JWT_SECRET=gere-secret-unico-para-jwt
ENCRYPTION_KEY=gere-chave-unica-para-encryption

# ------------------------------------------------------------------------------
# DATABASE (Railway PostgreSQL)
# ------------------------------------------------------------------------------
DATABASE_CLIENT=postgres
# DATABASE_URL é injetado automaticamente pelo Railway
# Configurações adicionais (opcional):
DATABASE_SSL=true
DATABASE_SSL_REJECT_UNAUTHORIZED=false
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10

# ------------------------------------------------------------------------------
# SERVER
# ------------------------------------------------------------------------------
# Railway injeta PORT automaticamente, não precisa definir
NODE_ENV=production
HOST=0.0.0.0

# ------------------------------------------------------------------------------
# URLS PÚBLICAS (ajuste para seu domínio)
# ------------------------------------------------------------------------------
# Staging
PUBLIC_URL=https://cfatech-cms-staging.up.railway.app
# ou com domínio customizado:
# PUBLIC_URL=https://cms-staging.cfatech.com.br

# Production
# PUBLIC_URL=https://cms.cfatech.com.br

# ------------------------------------------------------------------------------
# CORS (domínios do frontend permitidos)
# ------------------------------------------------------------------------------
# Staging
CORS_ORIGIN=https://cfatech-staging.vercel.app,http://localhost:9002

# Production
# CORS_ORIGIN=https://cfatech.com.br,https://www.cfatech.com.br

# ------------------------------------------------------------------------------
# UPLOADS
# ------------------------------------------------------------------------------
# Local (Railway Volumes) - não recomendado para produção
UPLOAD_PROVIDER=local

# AWS S3 (recomendado para produção)
# UPLOAD_PROVIDER=aws-s3
# AWS_ACCESS_KEY_ID=
# AWS_ACCESS_SECRET=
# AWS_REGION=sa-east-1
# AWS_BUCKET=cfatech-cms-uploads

# Cloudinary (alternativa)
# UPLOAD_PROVIDER=cloudinary
# CLOUDINARY_NAME=
# CLOUDINARY_KEY=
# CLOUDINARY_SECRET=

# ==============================================================================
# COMO CONFIGURAR NO RAILWAY
# ==============================================================================
#
# 1. Crie um novo projeto no Railway
# 2. Conecte o repositório GitHub (CFA-Tech/cfatech-cms)
# 3. Adicione um serviço PostgreSQL (Railway → Add Service → Database → PostgreSQL)
# 4. Configure as variáveis acima em Railway → Project → Variables
# 5. Deploy será feito automaticamente
#
# Gerar chaves seguras:
#   openssl rand -base64 32
#   openssl rand -base64 16 (para APP_KEYS, gere 4)
#
# ==============================================================================
