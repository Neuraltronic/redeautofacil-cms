# ==============================================================================
# CFATech CMS - Variáveis de Ambiente
# ==============================================================================
# Copie este arquivo para .env e preencha com seus valores
# NUNCA commite o arquivo .env no repositório!
# ==============================================================================

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
HOST=0.0.0.0
PORT=1337

# Ambiente (development, staging, production)
# NODE_ENV=production

# ------------------------------------------------------------------------------
# Security Keys (OBRIGATÓRIO - Gere novos para produção!)
# ------------------------------------------------------------------------------
# Use: openssl rand -base64 32 para gerar cada chave

# APP_KEYS: 4 chaves separadas por vírgula (para assinatura de cookies)
APP_KEYS=toBeModified1,toBeModified2,toBeModified3,toBeModified4

# Salt para tokens de API (afeta todos os tokens de API)
API_TOKEN_SALT=tobemodified

# Secret JWT do painel admin
ADMIN_JWT_SECRET=tobemodified

# Salt para tokens de transferência (export/import de dados)
TRANSFER_TOKEN_SALT=tobemodified

# Secret JWT geral (autenticação de usuários via API)
JWT_SECRET=tobemodified

# Chave de encriptação (dados sensíveis)
ENCRYPTION_KEY=tobemodified

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------

# ----- SQLite (Desenvolvimento) -----
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# ----- PostgreSQL (Produção) -----
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=cfatech_cms
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=sua_senha_segura
# DATABASE_SSL=false
# DATABASE_SSL_REJECT_UNAUTHORIZED=false

# ----- MySQL (Alternativa) -----
# DATABASE_CLIENT=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_NAME=cfatech_cms
# DATABASE_USERNAME=strapi
# DATABASE_PASSWORD=sua_senha_segura

# ------------------------------------------------------------------------------
# Upload Configuration
# ------------------------------------------------------------------------------

# Provider de upload (local, aws-s3, cloudinary)
# UPLOAD_PROVIDER=local

# ----- AWS S3 -----
# UPLOAD_PROVIDER=aws-s3
# AWS_ACCESS_KEY_ID=
# AWS_ACCESS_SECRET=
# AWS_REGION=sa-east-1
# AWS_BUCKET=cfatech-cms-uploads

# ----- Cloudinary -----
# UPLOAD_PROVIDER=cloudinary
# CLOUDINARY_NAME=
# CLOUDINARY_KEY=
# CLOUDINARY_SECRET=

# ------------------------------------------------------------------------------
# CORS Configuration (Produção)
# ------------------------------------------------------------------------------
# URL do frontend que pode acessar a API
# CORS_ORIGIN=https://cfatech.com.br

# ------------------------------------------------------------------------------
# Strapi Admin Configuration
# ------------------------------------------------------------------------------
# URL do admin panel (para redirecionamentos)
# ADMIN_URL=/admin

# Desabilitar telemetria (opcional)
# STRAPI_TELEMETRY_DISABLED=true

# ------------------------------------------------------------------------------
# Email Configuration (Opcional - para notificações)
# ------------------------------------------------------------------------------
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USERNAME=
# SMTP_PASSWORD=

# ------------------------------------------------------------------------------
# Logging (Produção)
# ------------------------------------------------------------------------------
# LOG_LEVEL=info

# ==============================================================================
# NOTAS DE SEGURANÇA
# ==============================================================================
#
# 1. NUNCA use os valores padrão em produção
# 2. NUNCA exponha este arquivo ou variáveis no frontend
# 3. Use secrets manager em produção (Vercel, AWS Secrets, etc.)
# 4. Rotacione chaves regularmente
# 5. API_TOKEN_SALT: Se alterado, TODOS os tokens de API serão invalidados
#
# Comandos úteis:
#   Gerar chave:      openssl rand -base64 32
#   Gerar APP_KEYS:   echo "$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16),$(openssl rand -base64 16)"
#
# ==============================================================================
